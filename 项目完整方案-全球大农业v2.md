# 全球大农业 AI 生态 · 完整方案 v2.0

> 本文整合你此前三份战略文档（《项目战略指引》《项目方案一稿》《项目执行方案》）+ 已建成代码资产，并针对你最新提出的「全球大农业五模块」重新锚定目标与阶段。
> 版本：v2.0｜日期：2026-09-04
> 性质：**战略定案稿**。技术架构、数据标准、运营手册以本文为母本展开。

---

## 〇、诊断：为什么目前的产品没构成你想要的效果

### 0.1 三层尺度断裂（核心问题）

| 层 | 之前定的目标 | 实际交付 | 断层性质 |
|---|---|---|---|
| **战略层**<br>（方案一稿） | 地球农业中轴 AgriAxis：全球资源接入 → AI 分析 → 最优配置输出；远期地外农业 | 无对应交付 | 宏大但无落地路径 |
| **执行层**<br>（执行方案） | Phase 0：家庭阳台 MVP，30 个个人用户 + 3 个小型农场，验证监控闭环与飞轮 | 未做用户运营，直接写 Agent Demo | **切口被完全绕过** |
| **产物层**<br>（现有代码） | — | 110 作物 × 6 气候带静态 JSON、四 Agent 规则引擎、病虫害关键词匹配、养分管理、Demo 站 + CI | 既不是全球基础设施，也不是有用户的产品 |

**结论**：做出来的东西是「技术方案 Demo」，不是「产品」。它证明的是「Agent 能跑通一个农业咨询流程」，而不是「有人用、有数据回、有钱进」。

### 0.2 真正的根因：飞轮从未转起来

你的《项目方案一稿》自己写得非常清楚：

> 「Phase 0 的使命：用最轻的验证回答『分布式种植的 AI 管家有没有人付费、能不能攒数据』。**数据飞轮一日不转，其他皆为空中楼阁。**」

但现状对照：

| 飞轮构成要件 | 战略要求 | 实际状态 |
|---|---|---|
| 真实用户 | 30 人 + 3 农场 | **0**（无注册、无用户体系） |
| 设备/数据接入 | 手机传感器 + 蓝牙设备 | **0**（无传感接入） |
| 数据回流 | 环境×操作×结果 三元组 | **0**（feedback_log 仅内测手工提交） |
| 北极星指标 | 监控周活跃率 ≥3 次/周 | **从未被测量** |
| 收入 | 订阅雏形 | **0**（无支付、无定价） |

> **判断**：功能代码写得越多，离目标越远——因为**每一分投入都在加固一个没有用户的空壳**。这不是执行不力，是目标与阶段未对齐的必然结果。

### 0.3 一个必须挑明的定位漂移

你的新表述（全球大农业）与《项目战略指引》的原有定位**不一致**，必须明确取舍：

| | 《战略指引》原定位 | 你本次的新表述 | 差异 |
|---|---|---|---|
| 主战场 | **L3 分布式农业**（阳台/社区/车载），明确「L1 大田已被巨头占据，**不做**」 | **全球大农业**：全球数据库 → 地形/土壤/气候要素分析 → 全球作物分区分类 | 从 L3 上移到 **L0/L1** |
| 数据重心 | 分布式真实种植数据（稀缺、开放增量源） | 宏观地理数据（气候/土壤/遥感） | 从「自建采集」转向「聚合开源」 |
| 竞争力来源 | 分布式数据池 = 模型训练上游 | 全球数据覆盖 + 分析能力 | 从「独占数据」转向「组织与输出方式」 |

**我的判断（不附和）**：

这两个方向**可以统一在「中轴」框架下，但必须分清资产与场景**：

- **全球数据底座 = 能力与资产**（可聚合开源、可自动化、不需要地推销售，个人项目做得动）；
- **落地变现必须选一个具体场景**（大农业 B 端生意要销售、要关系、要行业 know-how，个人项目极难啃）。

所以我推荐：**「全球数据底座 + 城市种植（生成箱）」双轨**——全球尺度做资产与差异化，城市可控环境做落地与变现。这也是你本次五模块里唯一能形成闭环的一段（模块 5）。

---

## 一、你之前设想的准确提炼（继承部分）

以下内容来自你三份历史文档，作为 v2.0 的继承基座，不重新发明。

### 1.1 原始愿景

> **让每一粒种子，找到最适合它的土地。**——地球农业中轴（AgriAxis）：广泛接入地球农业资源，通过 AI 分析，输出最优化配置。

远期延伸：同一套「环境 × 资源 → 最优种植方案」的求解逻辑，可延伸至月球/火星封闭生态农业（**仅作愿景，不投当期资源**）。

### 1.2 五大核心构件

| 构件 | 内容 |
|---|---|
| **① 一个抽象** | 种植单元 = `{环境数据 × 生长状态 × 操作记录 × 结果}`，个人阳台与万亩农场同构 |
| **② 六层架构** | L0 数据底座 → L1 感知 → L2 模型AI → L3 执行控制 → L4 业务支撑（链路）→ L5 消费服务 → L6 运营生态 |
| **③ 三大数据体系** | 环境数据（选址智能）/ 生长数据（种植管家）/ 链路数据（鲜度中枢） |
| **④ 四大切口** | 监控管理（入口·P0）→ 选址规划（加深·P1）→ 链路鲜度（变现·P2）→ 数据模型服务（收口·P3） |
| **⑤ 三大准则** | **自动化**（系统自转）/ **生态化**（开放多边）/ **盈利化**（每层有变现出口） |

**切口四准则**（选择做什么时用）：粘性 × 标准化 × 数据入口（飞轮）× 数据对接去人化。不符合的一律不做。

**不做清单**：不造硬件、不重资产生产、不做大宗价格战、不做依赖人工对接的环节。

### 1.3 已建成资产（可复用，不浪费）

| 资产 | 文件 | 状态 | 在 v2.0 中的角色 |
|---|---|---|---|
| 四 Agent 流水线 | `agent/climate|crop|growth|eco_agent.py` | 跑通，rubric 0.92 | 保留为「分析层」原型 |
| 病虫害诊断 | `agent/pest_agent.py` + `agent/vision.py` | 规则匹配 + 视觉可插拔 | 升级为模块 4 |
| 养分/阶段施肥 | `agent/nutrition_agent.py` | 四阶段方案 | 并入模块 5 配置引擎 |
| 统一编排路由 | `agent/orchestrator.py`（call_skill / list_skills） | 8 Skill | 保留为 Agent-native 接口雏形 |
| 可信层 | `core/trust_layer.py` | 证据链 + SHA256 签名 | **v2.0 增强：数据源可信分级** |
| 反馈飞轮 | `engine/flywheel.py` | 校准闭环已生效 | 需接真实用户数据才有意义 |
| 作物数据 | `data/crop_adapt_db.json`（110 种 × 6 带） | 静态 | 被模块 1 全球库取代/扩充 |
| Demo + Docker + CI | `app/`、`deploy/`、`.github/` | CI 绿 | 保留；公网部署仍待你执行 |

---

## 二、目标重新锚定

### 2.1 你本次五个模块的架构落位

| 你的模块 | 架构层 | 本质 | 差异化难度 |
|---|---|---|---|
| ① 全球农业数据库 | L0 | 数据底座（开源聚合 + 搜索补库） | 低（数据可得）→ **组织方式才是壁垒** |
| ② 地形/土壤/气候要素分析 | L0→L2 | 选址智能/适宜性评价 | 中（GAEZ 等已有公开方案） |
| ③ 全球农作物分区分类 | L0→L2 | 作物 × 环境 适宜性矩阵 | 中（需本地化校验） |
| ④ 病虫害识别 | L2 | 视觉模型 + 病虫害库 | 中高（需标注数据，可借开源集冷启动） |
| ⑤ 城市种植环境配置 → 生成箱 | L3 | **配置输出与执行闭环** | **高（且这是唯一能收钱的闭环）** |

### 2.2 关键判断：数据库本身不是壁垒（诚实提醒）

必须说清楚，否则会浪费大量时间：

- **「建一个全球农业数据库」这件事没有护城河**——FAO、WorldClim、ISRIC、CGIAR、GBIF 等机构已提供高质量开放数据，巨头（阿里云农业大脑、先正达、拜耳）也都有。
- **真正的壁垒在四件事**：
  1. **组织方式**：把分散数据源统一成「一个可查询的、单元化的、带可信度分级的知识体」；
  2. **可机读 / Agent-native**：不是给人看的网页，是给 Agent 调的 API/MCP（**这是你做 SwarmLabs Agent Infra 的天然主场**）；
  3. **输出可执行**：从「分析结果」走到「可直接下发给硬件的配置参数」（模块 5）；
  4. **闭环数据回流**：配置 → 执行 → 真实结果 → 校准模型（**这是开源数据永远拿不到的部分**）。

> 一句话：**搜集数据不难，难的是让数据变成「能输出可执行配置、并能用真实结果自我校准」的闭环。后者才是资产。**

### 2.3 推荐定位（v2.0 定案）

> **做「全球农业环境配置的知识底座与输出引擎」——聚合全球开放农业数据，统一为单元化、可机读、带可信分级的知识体；对全球尺度输出「种什么、哪里种」的分析，对城市/可控环境场景输出「可直接下发生成箱的环境配置配方」，并用执行结果回流校准模型。**

三句价值主张：

- 对**全球尺度**：任何一块地（经纬度）→ 可种什么、风险是什么（L0-L2）
- 对**城市种植**：作物 + 阶段 + 箱体条件 → 一套可下发执行的环境配置配方（L3）
- 对**Agent 生态**：全部能力以 API/MCP 暴露，让别人的 Agent 来接我们的数据（分发革命）

---

## 三、目标分层与北极星（v2.0）

> **原则**：每阶段只有一个北极星指标，阶段边界以「飞轮是否转起来」为准，不以时间硬切。

| 阶段 | 周期 | 一句话目标 | 北极星指标 | 决策门（不过关不进下一阶段） |
|---|---|---|---|---|
| **P0 数据底座** | 0-3 月 | 把全球开放数据聚成一个可查的知识体 | **可查询实体数**（作物 × 环境 × 病虫害） | ≥10 万条结构化实体，且 API 可稳定查询 |
| **P1 分析与分区** | 3-9 月 | 任意经纬度 → 可种什么 + 风险 | **适宜性评价准确率**（对照 FAO GAEZ/本地实测） | 主流 30 种作物评价与权威基线一致率 ≥80% |
| **P2 识别与配置** | 9-18 月 | 病虫害可识别；生成箱配置可下发 | **配置采纳率 + 真实种植单元数** | ≥50 个真实运行单元，配置被实际采用 |
| **P3 生态与标准** | 18-36 月 | 环境配置协议成为事实标准；数据服务变现 | **接入设备/伙伴数 + 数据服务收入** | 协议被 ≥3 家硬件厂采用，数据服务有付费客户 |

---

## 四、分阶段路线图（把五个模块落成阶段）

### P0｜数据底座（0-3 月）—— 对应模块 ①

**目标**：全球开放农业数据 → 统一 schema → 可查询知识体（Agent-native API）。

| # | 关键动作 | 交付物 |
|---|---|---|
| P0.1 | 定义统一数据 schema（作物档案 / 环境格网 / 病虫害 / 品种 / 配置配方），含**可信度分级**（权威机构 / 文献 / 社区 / 推断） | 数据标准文档 + JSON Schema |
| P0.2 | 接入全球开放数据源（清单见 §5.1），批量入库 | 数据接入管线 + 原始库 |
| P0.3 | **搜索补库 Agent**：对开源库缺口（尤其中国本土品种/病虫害）用搜索工具检索 → 结构化抽取 → 带来源入库 | 补库 Agent + 增量管线 |
| P0.4 | 统一查询 API + MCP 服务（让外部 Agent 可调） | API + MCP server |
| P0.5 | 把现有 `crop_adapt_db.json`（110 作物）迁移进新 schema，不丢资产 | 迁移脚本 |

**不做**：不做用户端、不做硬件、不做模型训练。

### P1｜分析与分区（3-9 月）—— 对应模块 ②③

**目标**：任意经纬度 → 作物适宜性 + 风险分析 + 全球分区分类。

| # | 关键动作 | 交付物 |
|---|---|---|
| P1.1 | 环境要素层构建：气候（WorldClim/CHELSA）+ 土壤（SoilGrids/HWSD）+ 地形（DEM）+ 土地覆盖 | 格网化环境要素库 |
| P1.2 | 作物环境需求模型（温度区间/降水/pH/光照/积温/耐逆性），对齐 FAO Ecocrop 思路 | 作物需求档案 |
| P1.3 | 适宜性评价引擎（多因子加权 + 限制因子否决），对照 GAEZ 校验 | 评价引擎 + 校验报告 |
| P1.4 | 全球农业分区分类（气候带 × 土壤型 × 地形 → 作物组），可交互浏览 | 分区图谱 + 查询界面 |
| P1.5 | 增强 `trust_layer`：每条建议标注数据源 + 可信等级（承接已有 AgriTrust） | 可信分级体系 |

### P2｜识别与配置（9-18 月）—— 对应模块 ④⑤

**目标**：病虫害可识别；城市种植环境配置可输出并下发到生成箱。

| # | 关键动作 | 交付物 |
|---|---|---|
| P2.1 | 病虫害库结构化（症状/寄主/环境诱因/防治/图像），对齐 EPPO、CABI | 病虫害知识库 |
| P2.2 | 视觉识别：先用开源数据集（PlantVillage / iNaturalist）冷启动，再做本土微调和严重度分级 | 识别服务（复用 `agent/vision.py`） |
| P2.3 | **环境配置配方引擎（Env Recipe）**：作物 × 生长阶段 × 箱体条件 → 温度/湿度/光照光谱与时长/CO₂/EC/pH/水肥 schedule/风速 | 配置引擎（由 NutritionAgent 升级） |
| P2.4 | **环境配置协议 v1**：定义配方的数据格式与下发接口（开源） | 协议规范 + SDK |
| P2.5 | 对接 1-2 款真实生成箱/种植柜，配置可一键下发，结果回流 | 硬件适配 + 回流管线 |
| P2.6 | 种子用户：50 个真实运行单元（个人/社区/教育） | 用户与真实数据 |

**这是唯一形成商业闭环的阶段**（配置即服务，可订阅/可分成）。

### P3｜生态与标准（18-36 月）—— 收口

| # | 关键动作 | 交付物 |
|---|---|---|
| P3.1 | 环境配置协议开源推广，争取硬件厂采用（种植版 Matter/HomeKit） | 开放协议 + 认证体系 |
| P3.2 | 数据/模型服务平台：脱敏数据报告、品种×环境表现、模型 API | 数据服务 |
| P3.3 | 链路鲜度中枢（原战略切口 3），若资源允许再启动 | 链路服务 |
| P3.4 | 碳汇计量与绿色资产（远期金融化） | 计量模块 |

---

## 五、五个模块的落地设计

### 5.1 模块 ①：全球农业数据库

**可用开放数据源（按类别）**

| 类别 | 数据源 | 用途 | 备注 |
|---|---|---|---|
| 气候 | **WorldClim 2.1**、CHELSA、ERA5 | 生物气候变量、逐月温降 | 项目已在用 WorldClim |
| 土壤 | **ISRIC SoilGrids**（250 m）、FAO HWSD | 质地/pH/有机碳/养分 | 项目已提到 ISRIC |
| 地形 | SRTM / ALOS World 3D / GMTED | 高程、坡度、坡向 | 免费 DEM |
| 土地覆盖 | ESA CCI Land Cover、GLC_FCS30、Copernicus | 耕地分布、作物类型 | 哨兵卫星衍生 |
| **作物适宜性** | **FAO GAEZ v4**（全球农业生态区划） | 「哪里能种什么」的权威基线 | **与模块 ③ 直接对口，强烈建议作为校验基线** |
| **作物环境需求** | **FAO Ecocrop** | 每种作物的温度/降水/pH/光照需求 | **与模块 ⑤ 配置引擎直接对口** |
| 品种/种质 | Genesys、GBIF、USDA GRIN | 品种性状、抗逆性 | 品种数据缺口大 |
| **病虫害** | **PlantVillage**（图像）、iNaturalist、**EPPO Global Database**、CABI | 病虫害识别与百科 | EPPO 含检疫性名录，合规价值高 |
| 生产统计 | FAOSTAT、USDA FAS | 产量/面积/贸易 | 宏观验证 |
| 农艺知识 | CGIAR、各国农技推广站、CNKI/万方（中文） | 栽培规程、本地化经验 | 中文部分需补库 Agent |

> ⚠️ **诚实标注**：以上均为业界主流的公开数据源，我基于训练知识列出；**正式接入前需逐项核实当前 URL、许可条款（部分为 CC-BY / 部分限非商业用途）与下载可达性**（尤其境外源在国内网络的连通性）。建议我后续用联网检索逐项验证后再定接入优先级。

**补库机制（你提到的「搜索工具数据入库」）**

```
缺口识别（schema 字段为空 / 置信度低）
  → 搜索检索（多源：学术/农技站/百科/论坛）
  → 结构化抽取（Agent 抽取为 schema 字段）
  → 可信度打标（权威机构 ★★★ / 文献 ★★ / 社区 ★ / 推断 ?）
  → 冲突仲裁（多源不一致时取高可信 + 保留分歧记录）
  → 入库 + 记录 data_lineage（来源 URL + 抓取时间）
  → 定期复检（源失效/数据更新自动标记）
```

**关键设计**：`data_lineage`（血缘）字段必须保留——这既是 AgriTrust 可信层的延伸，也是未来数据变现时证明合规与来源的依据。

### 5.2 模块 ②：地形/土壤/气候要素分析

- **方法**：格网化（建议 1 km 基础格网，城市场景可细化到 30 m）→ 多因子叠加 → 限制因子否决 + 加权评分。
- **输出**：地块画像（气候带 / 土壤型 / 地形 / 水源 / 限制因子）+ 风险提示（霜冻、干旱、涝害、盐碱）。
- **校验**：与 FAO GAEZ 分区对照，一致率作为 P1 北极星。

### 5.3 模块 ③：全球农作物分区分类

- **双维度**：① 环境分区（气候带 × 土壤型 × 地形）→ ② 作物组（按环境需求聚类）。
- **产出**：`作物 × 环境单元` 适宜性矩阵（适宜 / 次适宜 / 不适宜 + 限制原因）。
- **差异化机会**：公开方案（GAEZ）偏宏观大田，**补上「城市/微环境/设施种植」这一层**是空白——阳台朝向、箱体容积、补光条件等微观因子无人做。

### 5.4 模块 ④：病虫害识别

- **冷启动**：直接用 PlantVillage（5 万+ 叶片图像，公开）+ iNaturalist，不自建数据集。
- **本土化**：中国常见病虫害与症状表述（中文）是开源集的缺口 → 用补库 Agent + 社区上传逐步积累，**这是能形成独占数据的地方**。
- **架构**：复用现有 `agent/vision.py`（已支持 `AGRI_VISION_*` 可插拔后端），识别结果与病虫害知识库（EPPO/CABI 结构化）联动输出防治建议 + 安全约束。
- **注意**：检疫性病虫害（如黄龙病、红火蚁）必须上报，**不可只给防治建议**——现有 `SAFETY_CONSTRAINTS` 已包含，需持续强化。

### 5.5 模块 ⑤：城市种植环境配置 → 生成箱（**核心变现闭环**）

这是五个模块里**唯一能收钱**的一段，要重点设计。

**核心概念：环境配置配方（Env Recipe）**

```
输入：作物 + 品种 + 生长阶段 + 箱体条件（容积/光源/通风/是否封闭）+ 目标（速生/风味/观赏）
输出：一套可执行、可下发、可分阶段演进的参数集

温度：昼/夜 设定值 + 允许波动带
湿度：RH 目标区间 + 除湿/加湿触发阈值
光照：光谱配比（红蓝比/远红）+ PPFD 目标 + 光周期时长 + 补光启停时刻
CO₂：目标浓度 + 补充时机（仅在光照期）
水肥：灌溉频次/单次量 + EC 目标 + pH 目标 + 各阶段 NPK 配比（承接 NutritionAgent）
气流：风速/换气频次（防徒长、防病害）
阶段切换：按积温/天数/视觉判定的阶段跃迁条件
异常处理：高温/缺光/病害预警时的降级配置
```

**为什么这是壁垒**：
- 开源数据给得了「番茄适宜温度 20-25℃」，给不了「你这个 40 L 箱体、LED 补光 100 μmol、杭州朝北阳台、种千禧樱桃番茄，第 3 周该怎么设」；
- 每个真实箱体的执行结果回流，都是**别人拿不到的校准数据**（这正是 P2 北极星「配置采纳率 + 真实单元数」的意义）。

**输出形态**（三层，价值递增）：
1. **人读**：配置卡片（前端展示，现有 UI 可承接）；
2. **机读**：Env Recipe JSON（可被任何系统解析）；
3. **可执行**：通过协议直接下发到生成箱控制器（L3 执行闭环）。

---

## 六、未来农业生态体系：值得提早布局的 7 个关键节点

按「卡位价值 × 当下成本」排序。每个节点给出**现在就能做的低成本埋点**。

| # | 关键节点 | 为什么是节点 | 现在能做什么（低成本埋点） | 成本 |
|---|---|---|---|---|
| **1** | **环境配置协议**（种植版 Matter / HomeKit） | 硬件厂各自为政，**谁来定义「配置配方」的标准格式，谁就卡住所有种植硬件**。你不造硬件，只定义接口——与「不做硬件」原则完全一致 | 写 Env Recipe v1 规范 + 开源 + 提供解析 SDK；在 P2 对接硬件时强制走这套格式 | **极低**（写文档） |
| **2** | **Agent-native 农业数据服务**（API / MCP） | AI 时代的分发革命：未来的流量入口是 Agent 而非网页。让别人（开发者/硬件厂/研究机构）的 Agent 来接你的数据，比自己做 App 获客便宜得多。**这是你做 SwarmLabs Agent Infra 的天然主场，是差异化最强的一项** | P0 就把数据以 MCP server 暴露；写 Agent 友好的文档与示例 | 低（与 P0 同步做） |
| **3** | **品种 × 环境表现数据** | 种子公司最想知道「哪个品种在哪个环境表现最好」——高价值 B 端数据，且能自然从配置闭环里长出（你有了 50 个真实单元，就有了别人没有的表现数据） | 数据 schema 里预留 `品种 × 环境 × 结果` 字段；P2 起系统记录 | 低（埋字段） |
| **4** | **病虫害本土化视觉数据集** | 开源集（PlantVillage）以国外作物/病害为主，**中国常见病虫害的中文标注图像是真空白**，且越积越值钱（可授权给硬件厂/农资商） | 每次用户上传诊断图像即沉淀样本（需授权）；建图像库目录与标注规范 | 中（需用户量） |
| **5** | **碳汇与绿色资产计量** | 分布式/城市种植的碳减排未来可交易、可进绿色金融。周期长，但**计量字段现在就得埋，事后补极难** | schema 预留碳汇相关字段（生物量/能耗/减排估算） | **极低**（埋字段） |
| **6** | **城市食物韧性 / 政策接口** | 城市农业、社区支持农业（CSA）、学校食育均有政策窗口；政府与城市合作是低竞争高壁垒的通道 | 输出「城市可种什么」的公共报告、学校食育方案；关注地方政策 | 低（内容输出） |
| **7** | **地外 / 极端环境封闭生态农业** | 与你原始「中轴」愿景一脉相承，叙事价值与品牌溢价高；同一套配置引擎天然适用于封闭舱。**但绝不能投当期资源** | 仅在架构上保证「配置引擎不依赖地表/自然光」，保持可迁移性 | 零（仅架构约束） |

> **排序建议**：**1、2、5 立刻做（成本近零，卡位价值高）；3、4 随 P2 自然长；6 用内容输出低成本试探；7 只作叙事，不投资源。**

---

## 七、下一步：建议的 30 天动作

| 优先级 | 动作 | 产出 | 对应 |
|---|---|---|---|
| **P0-A** | 定数据 schema v1（作物/环境/病虫害/品种/配方五类 + 可信分级 + 血缘字段） | 数据标准文档 | 模块 ① 地基 |
| **P0-B** | 核实全球数据源清单（逐项验证 URL / 许可 / 国内可达性） | 可用源清单（标注可达性） | 模块 ① |
| **P0-C** | 写 Env Recipe v1 协议规范（节点 1，成本最低卡位最高） | 协议文档 + JSON Schema | 关键节点 1 |
| **P0-D** | 把现有数据 API 化 + 起一个 MCP server（节点 2） | MCP server | 关键节点 2 |
| **P0-E** | 现有 110 作物迁移进新 schema（不丢资产） | 迁移脚本 | 承接存量 |

**明确暂缓**：
- ❌ 继续堆 Agent 功能（没有用户与数据，堆功能只加固空壳）
- ❌ 大农业 B 端销售（个人项目啃不动，且违背「去人化」准则）
- ❌ 自建视觉数据集（先用 PlantVillage 冷启动）
- ⏸ 公网部署（可做，但优先级低于以上；且部署卡在你 SSH 那一步）

---

## 附：与旧文档的关系（继承 vs 修正）

| 旧文档内容 | v2.0 处理 |
|---|---|
| 三大准则（自动化/生态化/盈利化） | **完全继承** |
| 切口四准则（粘性/标准化/数据入口/去人化） | **完全继承** |
| 种植单元抽象 `{环境×生长×操作×结果}` | **继承**，扩展为统一 schema |
| 七层架构 | **继承**，作为分层参照 |
| AgriTrust 可信层 | **继承并增强**：加数据源可信分级 + 血缘字段 |
| 四大切口（监控→选址→链路→数据服务） | **修正**：链路鲜度（切口 3）后移至 P3 可选；主序列改为 **数据底座 → 分析分区 → 识别配置 → 生态标准** |
| 主战场 L3 分布式 | **修正为**「全球数据底座（资产）+ 城市种植（场景）」双轨 |
| Phase 0 家庭阳台 30 用户 | **修正**：用户验证后置到 P2，与生成箱闭环合并（否则飞轮无载体） |
| 地外农业 | **保留为远期愿景，明确不投当期资源** |

---

> **一句话收口**：你的原始直觉是对的——**全球大农业的数据与配置是真正的中轴**。但这件事的顺序必须是：**先把数据聚成可机读的知识体（P0），再让它能回答「种什么」（P1），再让它能直接指挥硬件（P2），最后把配置格式变成行业标准（P3）**。在此之前写的任何功能，都只是在给一个没有用户的空壳刷漆。
